# Cortex 宣言

*An operating system for AI agents. / 一个给 AI Agent 用的操作系统。*

> **状态：** `v0.2.1` —— 文档承诺的四个 v0 缺口现已全部补上：`sleep()`
> 真的会挂起进程，restore 出来的进程自己会跑，同步 syscall 开始写进日志；`0.2.1`
> 把第四件事也补上了——持久化布局现在是 ARCHITECTURE §7 描述的每进程子树
> （`processes/<pid>/{log.crec, meta.json, checkpoints/}`）。另外新增
> `cortex top`，一个陌生人五分钟能看懂的视图。Phase 0–5 全部完成：内核能启动，
> 七个驱动全部交付，CLI 完整，三个 demo 端到端跑通。这份文档讲的是**为什么**，
> 它没有变过。

> 英文原文见 [MANIFESTO.md](./MANIFESTO.md)。若两者有出入，以英文版为准。

---

## 一、批处理时代

今天所有的「Agent 框架」都把 Agent 当成一个**函数**。你用任务调用它，它跑到结束，
它返回。这里面没有「跨时间存在的 Agent」这个概念，没有「两个 Agent 分担工作」，
没有暂停、恢复、fork、kill，没有资源限制，没有权限，没有监督。

我们其实来过这里。

1965 年，你把一个作业提交给主机，然后等操作员把打印结果递回给你。1975 年，Unix 给了
我们**进程** —— 一个正在运行的程序，作为一等实体，拥有身份、状态、生命周期、父子关系。
从那以后，计算变得可组合了。后面发生的一切 —— 网络、工作站、云、手机 —— 都建立在
这个抽象之上。

AI Agent 至今仍处在批处理时代。它们是作业，不是进程。它们是脚本，不是操作系统的公民。

**Cortex 就是 AI Agent 的那一个 Unix 时刻。**

---

## 二、我们相信什么

### 1. Agent 是进程，不是函数。

它有身份（PID）。它有状态（记忆、注意力、意图）。它有一个可以活得比调用方更久的
生命周期。它可以被 spawn、暂停、fork、发信号、kill、checkpoint、restore。它可以有
一个监督它的父进程，也可以有它自己监督的子进程。它死的时候，父进程回收它；父进程死的
时候，它被重新挂到 `init` 上。

这不是一个比喻。这是真实的抽象。

### 2. 要 syscall，不要魔法。

Agent 做的每一件有意义的事都是一次显式的 syscall：`llm_call`、`tool_call`、
`memory_read`、`memory_write`、`spawn`、`send`、`recv`、`checkpoint`、`restore`。
每一次都被记录。每一次都可以被拦截、mock、限流、拒绝。

我们拒绝那种「AI 魔法」式的框架设计 —— 在那里 *「Agent 决定要……」* 被当成一种解释。
不。这个 Agent 在 UTC 14:23:04 调用了 `tool_call(grep, "TypeError")`，我们手里有凭据。

### 3. 小内核，大用户态。

Cortex 内核只有 **3,000 行 TypeScript**。其他一切都是用户态程序。

- LLM provider 是驱动。
- 工具协议（MCP、OpenAPI、自定义）是驱动。
- 记忆后端（SQLite、Qdrant、文件）是驱动。
- Agent 框架 —— dsh、LangChain、AutoGen、smolagents —— 是**用户态运行时**，
  就像跑在 Linux 上的 Python 或 Node。

它们应该跑在 Cortex **之上**，而不是和它竞争。一个做得太多的内核，就是一个什么都做不
好的内核。

### 4. 组合才是杀手级特性。

一个系统，如果它能像 Unix 管道一样 `planner | coder | reviewer` 地串起来，如果其中一个
Agent 能 spawn 并监督一百个子进程，如果任意 Agent 在任意时刻都能 `fork()` 去试另一条
路 —— 那这个系统能做到任何单体框架永远做不到的事。

Unix 赢不是因为更聪明，而是因为一切都是可组合的。Cortex 押的是同一匹马。

### 5. 确定性不可让步。

每一次 syscall 都被记录。每一次状态迁移都被记录。每一次执行都可回放。每一个时间点都
可 fork。

调试一个 Agent 不应该比调试一个普通程序更难。如果你没法用一个调用栈和一份 syscall 日志
回答 *「它为什么这么干？」*，那你手里就不是一个系统，而是一台老虎机。

### 6. 无聊的技术。

TypeScript，因为它无处不在。SQLite，因为它可靠。POSIX 形状的抽象，因为它们承载了
五十年的积累。

我们不是来发明一门新语言、一个新数据库、或一种新的计算模型的。我们是来把**已有的**
计算模型，应用到一个不知怎么地一直在没有它的情况下运转的领域。

### 7. 公开构建。

每一个设计决策都写在 repo 里。每一个错误都留在 git 历史里。每一次争论都在公开 issue
里解决。仓库是唯一事实来源，而不是某个私有的 Slack 频道。

如果你读不到源码，你就不理解这个系统。

---

## 三、我们拒绝什么

**什么都往里塞的框架。** LangChain 教会了一代人：一个 Agent 框架应该有两百个抽象 ——
chains、agents、tools、retrievers、memory、callbacks、outputs、parsers、embeddings、
vectorstores、document loaders、text splitters，如此无限。合适的抽象数量更接近十二个。
我们会把它们命名、精确定义，然后停下。

**隐藏状态。** 如果你的框架的行为取决于你没法检视、修改、或做版本管理的内部状态，
那它就不是框架，而是一个多绕了几步的黑箱。

**供应商锁定。** Cortex 能跑在任何 LLM provider、任何工具协议、任何记忆后端上。
驱动接口就是契约。如果某个供应商插不进来，那是我们驱动模型的 bug —— 不是那个供应商的
特性。

**「AI 帮你搞定一切」式的 UX。** Cortex 是给那些想*理解并掌控*自己 Agent 的构建者用的。
如果你想要魔法，市面上有很好的产品，但它们不是我们。

**过早的分布式。** 没有 Kubernetes operator，没有托管云服务，没有 SaaS，没有融资。
内核要么能跑在一台笔记本上，要么就不跑。

---

## 四、为什么不是……

Cortex 不是第一个想要「持久、可组合、可调试的 Agent」的项目。你完全有理由问：这些项目
都有了，我们为什么还要存在。

**为什么不是 Temporal？**
Temporal 极其擅长持久执行 —— 但它的模型是*「人把工作流写成代码；引擎保证这段代码在崩溃
后能活下来」*。决策是由程序员预先定好的，引擎只负责让它们可靠。Cortex 的模型不同：
**Agent 在运行时通过 LLM syscall 做决策。** 决策图是涌现出来的，不是声明出来的。你完全
可以把 Cortex 建在 Temporal 之上，把它当持久化后端 —— 那是一个驱动层的决定，不是架构层
的决定。

**为什么不是 LangGraph？**
LangGraph 构建图。你声明节点和边，它来跑。但拓扑在写代码时就固定了。一个会决定
*「我要 spawn 三个子 Agent，等它们，然后合并它们的发现」* 的 Agent，没法是一张静态图 ——
**spawn 这个动作本身就是运行时的决策**。Cortex 说：没有图。只有进程。进程之间动态地
互相 spawn。所谓「图」，只是进程树在运行时恰好长成的那个形状。

**为什么不是 Kubernetes？**
Kubernetes 监督的是容器，不是认知。它能重启一个 pod，但它没法告诉你一个 Agent 为什么
决定去调那个工具。它没有 LLM 预算、工具副作用、推理状态这些概念。你可以把 Cortex 跑在
k8s 上，每个 Cortex 内核变成一个 pod。k8s 既不知道、也不关心里面装的是什么。

**为什么不就做一个库？**
**我们就是一个库** —— TypeScript 写的，能从 npm 装。说自己是「OS」不是为了部署形态，
而是为了**抽象的纪律**。syscall、进程、信号、IPC：这些概念经过五十年的打磨。我们是在
继承那份打磨，而不是发明新的抽象。一个自称 OS 的库，是在许下一个承诺：这些抽象是通用的、
可组合的、小到能装进你脑子里。我们打算守住这个承诺。

**没人占过的那个位置**，就是我们要站的：**第一个把 LLM 决策当作一等可调度单元的系统。**
不是人声明好的工作流里的一个个步骤，不是集群里的一个个容器，而是**进程** —— 带着这个词
所意味着的全部纪律。

---

## 五、成功长什么样

**12 个月后。** 深圳的一个开发者 spawn 一个 Cortex daemon 帮她盯收件箱，再一个审她的
PR，第三个起草她的周报。它们跑在一台 5 美元的 VPS 上，能扛过重启，彼此通过 IPC 通信，
而不是通过某个 Slack 集成。每月 LLM 总成本：12 美元。它们都不是 LangChain agent。
她写了四十行 TypeScript。

**3 年后。** 每一个认真的 Agent 产品，都跑在 Cortex 或某个派生内核上。*「它兼容 Cortex
吗？」*成了开发者在采纳任何 Agent 工具之前会问的问题。syscall ABI 有三国语言的文档。
有一个小型会议。至少有两篇学术论文引用了这套设计。有一个健康的第三方驱动生态。

**10 年后。** 有人写一部 AI 基础设施史。Cortex 得到一段。也许两段。

---

## 六、谁应该加入

- 那些看着 LangChain 想 *「这不是计算机该有的样子」* 的系统工程师。
- 厌倦了为每个项目重写同一套编排胶水的 Agent 开发者。
- 想要一个新游乐场、在那里抽象真的重要的 OS 极客。
- 想要一个确定、可回放的基底来研究 Agent 行为的研究者。
- 想争论「Agent 到底是什么」、并且愿意跟会把它造出来的人争论的哲学家。

---

## 七、接下来是什么

这份文档是**为什么**。接下来的文档是**怎么做**：

| 文档 | 作用 | 状态 |
|---|---|---|
| `docs/STATE.md` | Agent 状态*到底是*什么；fork 与 checkpoint 的语义 | **已实现 —— 先读这个** |
| `docs/PROCESS.md` | Agent 生命周期与状态机 | **已实现 —— 8 个状态、12 个迁移** |
| `docs/ABI.md` | syscall 契约，以 TypeScript 类型给出 | **已实现 —— 19 个 syscall** |
| `docs/ARCHITECTURE.md` | 内核模块与数据流 | **已实现 —— 11 个模块、完整 syscall 生命周期** |
| `docs/HACKING.md` | 如何贡献、如何写一个驱动 | **已完成** |
| `docs/COOKBOOK.md` | 配方：监督、暂停/恢复、fork、daemon | **已完成** |
| `BACKLOG.md` | 最初的那些 issue，按优先级排 | 持续更新 |

然后就是代码 —— 而代码已经写完了。**Phase 0–5 全部完成，并已作为 [`v0.1.0`](./CHANGELOG.md) 发布。**

---

*Cortex 是一个正在使用的暂定名，背后是一个正在使用的想法。如果你读到了这里，
而且手指已经痒着想反驳，开一个 issue。每一封我们都会读。*
